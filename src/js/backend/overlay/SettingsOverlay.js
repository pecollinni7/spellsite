const OverlayBase = require('./OverlayBase');


module.exports = class SettingsOverlay extends OverlayBase
{

    constructor(overlayManager) {super(overlayManager);}

    show()
    {
        super.show();
    }
    hide()
    {
        super.hide();
    }
    init()
    {
        super.init();
    }
}